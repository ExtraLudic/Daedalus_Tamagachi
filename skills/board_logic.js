const _ = require("underscore");

module.exports = function(controller) {
  
  controller.on("button_click", function(params) {
    
    var bot = params.bot;
    var message = params.message;
    var user = params.user;
    
    
    
  });
  
}